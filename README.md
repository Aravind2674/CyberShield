The push includes:

main.py (FastAPI Server & Routes)
pipeline.py (YOLOv5, DeepFace FRS, EasyOCR ANPR Pipeline)
templates/index.html (The Professional Dark-Mode Dashboard)
requirements.txt (All exact AI and backend dependencies)
.gitignore
Anyone cloning the repository just needs to run pip install -r requirements.txt and then python main.py to get the exact same system running on their machine.
